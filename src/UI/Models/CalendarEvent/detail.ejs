<p>
  <%= start.toLocaleDateString('en-us', {
    day: 'numeric',
    weekday: 'long',
    month: 'long'
  }) %><br />
  <%= start.toLocaleTimeString('en-us', {
    hour: 'numeric',
    minute: 'numeric'
  }) %>&nbsp;&ndash;&nbsp;<%= end.toLocaleTimeString(
    'en-us',
    { hour: 'numeric', minute: 'numeric' }
  ) %>
</p>
<p><%= data.event.location %></p>
<p><%- data.event.cleanDescription %></p>